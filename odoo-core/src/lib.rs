pub mod key_store;
pub mod helpers;
pub mod key_listeners;
pub mod parser;
pub mod executor;
pub mod encoder;

pub fn add(left: usize, right: usize) -> usize {
    left + right
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn it_works() {
        let result = add(2, 2);
        assert_eq!(result, 4);
    }
}
